{
  "framework": "vite",
  "buildCommand": "echo 'Build skipped - already built in GitHub Actions'",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/proxy"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
