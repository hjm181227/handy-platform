name: Deploy Web App to Vercel (Staging)

on:
  push:
    branches:
      - develop
    paths:
      - 'packages/web/**'
      - 'packages/shared/**'
      - '.github/workflows/deploy-web-staging.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build:shared

      # 타입 게이트: 웹 tsc 오류 0 유지 (build:stage 는 tsc 를 돌리지 않으므로 별도 스텝)
      # TODO: 병렬 진행 중인 채팅 작업(useChat/useCustomOrderFlow 수정분) 커밋 후
      #       continue-on-error 제거해 필수 검사로 전환
      - name: Type check web app
        continue-on-error: true
        run: node node_modules/typescript/bin/tsc --noEmit -p packages/web/tsconfig.json

      - name: Build web app for staging
        run: npm run build:stage
        working-directory: packages/web
        env:
          VITE_API_BASE_URL: https://api.stage-handy.com
          VITE_CHAT_API_URL: https://chat.stage-handy.com
          VITE_ENVIRONMENT: staging
          VITE_TOSS_CLIENT_KEY: ${{ secrets.VITE_TOSS_CLIENT_KEY_STAGING }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_KAKAO_APP_KEY: ${{ secrets.VITE_KAKAO_APP_KEY }}

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Deploy to Vercel Staging
        run: |
          echo "🚀 Deploying to staging environment..."

          DEPLOYMENT_URL=$(vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }} --cwd packages/web)

          echo ""
          echo "✅ Staging deployment successful!"
          echo "📦 Deployed to: $DEPLOYMENT_URL"
          echo "🌐 Domain: www.stage-handy.com"
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_STAGING }}
